# ligolo

## ligolo - reach local services from an attacker standpoint
```
To reach a local service, add the magic ip  : sudo ip route add 240.0.0.1/32 dev ligolo
then use that IP to reach "localhost" for that machine
```
